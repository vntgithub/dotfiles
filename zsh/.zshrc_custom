export PATH="${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH"

# export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm
export NVM_DIR="$HOME/.nvm"
# Lazy-load nvm: source only on first use of nvm/node/npm/npx
_load_nvm() {
  unset -f nvm node npm npx _load_nvm
  [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
  [ -s "$NVM_DIR/bash_completion" ] && . "$NVM_DIR/bash_completion"
}
nvm()  { _load_nvm; nvm "$@"; }
node() { _load_nvm; node "$@"; }
npm()  { _load_nvm; npm "$@"; }
npx()  { _load_nvm; npx "$@"; }
export GOPATH=$HOME/go

export PATH=$PATH:$GOPATH/bin

export PATH="$HOME/.local/bin:$PATH"

export PATH=/home/vntrieu/.opencode/bin:$PATH

# pnpm
export PNPM_HOME="/home/vntrieu/.local/share/pnpm"
case ":$PATH:" in
  *":$PNPM_HOME:"*) ;;
  *) export PATH="$PNPM_HOME:$PATH" ;;
esac
# pnpm end

export PATH="$HOME/.local/share/nvim/mason/bin:$PATH"
alias lzd='lazydocker'
alias lzg='lazygit'

# export VAAPI_MPEG4_ENABLED=true
# export LIBVA_DRIVER_NAME=nvidia
# export NVD_BACKEND=direct
# export MOZ_DISABLE_RDD_SANDBOX=1 # Required for Firefox, but occasionally helps Chromium forks
#
# export MOZ_ENABLE_WAYLAND=1
# export VAHEADER_ALLOW_CROSS_COMPILATION=1
# export LIBVA_DRIVER_NAME=nvidia
# export NVD_BACKEND=direct
# Fix slow bracketed paste rendering in Zsh
zstyle ':bracketed-paste-magic' active-widgets '.self-*'
